#include "Poly.h"
#include "List.h"

//Poly::Poly()
//{
     //coeffList = new List<float>();
//}

void Poly::insert(float arr[],int len){
    for(int i = 0;i<len;i++){
   //     coeffList.addFront(arr[i]);
    }
}
string Poly::multiply(Poly* p2){
    Poly* result = new Poly();

  //  result->coeffList->addFront((coeffList->getEnd())*(p2->coeffList->getEnd()));
}
float Poly::evaluate(float x){
}
string Poly::differentiate(){
}
string Poly::toString(){
}
